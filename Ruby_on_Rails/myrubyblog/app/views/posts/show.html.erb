<h1><%= @posts.title %></h1>
<small><%= @posts.created_at.strftime("%b %d. %Y") %></small>
<p><%= @posts.body %></p>
<%= link_to "Edit Post", edit_post_path %> | <%= link_to "Delete", @post, data: { confirm: "Are you sure?" }, :method => :delete %>